from setuptools import setup

setup(
    name="enhancing",
    version="0.1dev",
    description="",
    author="",
    author_email="",
    packages=["enhancing"],  # same as name
    install_requires=[],  # external packages as dependencies
)
